1) The first piece of code helps to merge the two data sets(training and test) into one whole data set.
2) The second piece of code extracts only the measurements on the mean and standard deviation for each measurement.
3) The third piece of code uses descriptive activity names to name the activities in the data set
4) The fourth piece of code appropriately labels the data set with descriptive variable names.
5) The fifth piece of code creates a second, independent tidy data set with the average of each variable for each activity and each subject.